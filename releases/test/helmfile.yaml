environments:
  {{ .Environment.Name }}:
    values:

repositories:
  - name: dysnix
    url: https://dysnix.github.io/charts

release:
  - name: {{ .Values | get "release.name" "test" }}
    namespace: {{ .Values | get "release.namespace" "default" }}
    chart: dysnix/app
    values:
    -
      name: test-app-nginx
      image:
        repository: nginx
